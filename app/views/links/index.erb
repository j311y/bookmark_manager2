<h1> Links </h1>

<!-- Welcome <%= @user.email %> -->
<ul id='links'>
  <% @link.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
      Tags:
      <% link.tags.each do |tag| %>
      <%= tag.name %>
      <% end %>
    </li>

  <% end %>
</ul>
